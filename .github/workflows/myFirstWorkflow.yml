name: Schedule Test

on:
  push:

jobs:
  test1:
    runs-on: ubuntu-latest
    steps:
      - name: Before
        run: |
          pwd
          ls -ltr
          hostname
      - name: Checkout
        uses: actions/checkout@v7.0.1
      - name: After
        run: |
          pwd >> artifact.txt
          mkdir artifacts
          echo `ls -ltr` >> artifacts/list.txt
          echo `hostname` >> artifacts/hostname.txt
      - name: Upload file
        uses: actions/upload-artifact@v7.0.1
        with:
          name: single_file
          path: ./artifact.txt
      - name: Upload from directory
        uses: actions/upload-artifact@v7.0.1
        with:
          name: directory_artifacts_path
          path: artifacts/
      - name: Upload from multiple paths
        uses: actions/upload-artifact@v7.0.1
        with:
          name: multiple_paths
          path: |
            ./artifact.txt
            artifacts/
  
  test2:
    needs: test1
    runs-on: ubuntu-latest
    steps:
      - name: Check current directory
        run: |
          pwd
          ls -ltr
      - name: Download single file artifact
        uses: actions/download-artifact@v8.0.1
        with:
          name: single_file
      - name: Check current directory post download
        run: |
          pwd
          ls -ltr