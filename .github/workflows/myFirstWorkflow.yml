name: Schedule Test

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Before
        run: |
          pwd
          ls -ltr
          hostname
      - name: Checkout
        uses: actions/checkout@v6.1.0
      - name: After
        run: |
          pwd
          ls -ltr
          hostname
